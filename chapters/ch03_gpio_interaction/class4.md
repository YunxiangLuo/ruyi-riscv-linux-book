# Class 4: 按键消抖、状态机与可复用交互模块

## 本章目标

- 理解按键抖动和重复触发问题。
- 使用状态机组织 LED 模式切换。
- 将 GPIO 输入输出整理成后续项目可复用模块。

## 前置条件

- 已完成 GPIO 输出和输入实验。
- 已确认 LED/Button 板级参数。

## 知识简介

原始按键输入会受到抖动、重复采样和长按行为影响。状态机可以把“按键事件”和“LED 模式”分开，让交互逻辑更清楚，也方便 ch08 复用为模式切换、告警确认或手动触发。

## 环境准备

| 项目 | 要求 | 检查方式 |
| --- | --- | --- |
| GPIO 参数 | 已确认 | 板级记录 |
| 代码工程 | 可构建 | `make build` |
| 调试输出 | 可观察状态变化 | 终端日志 |

## 操作步骤

### 步骤 1：记录原始按键输入

观察按键按下和释放时的输出变化。

### 步骤 2：加入消抖

记录消抖前后的差异。

### 步骤 3：实现状态机

定义状态、事件和转移表。

## 运行验证

| 验证项 | 预期现象 | 记录 |
| --- | --- | --- |
| 消抖 | 单次按键不明显重复触发 |  |
| 状态机 | LED 模式按规则切换 |  |
| 调试输出 | 能解释状态变化 |  |

## 常见问题

### 按一次触发多次

现象：终端连续打印多次状态变化。

处理：检查采样周期和消抖时间。

## 本章成果

- 完成可复用 GPIO 交互模块。
