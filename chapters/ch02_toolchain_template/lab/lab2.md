# Lab 2: 工具链、Hello World、ELF 观察与 benchmark 边界

## 实验目标

- 编译并运行 Hello World。
- 使用 `file`、`readelf` 和 `ldd` 观察程序。
- 记录 benchmark 的选做边界。
- 记录一次运行失败或依赖差异的排查过程。

## 实验任务

1. 编写 Hello World。
2. 执行编译命令并保存输出。
3. 在目标系统运行程序。
4. 使用 `file`、`readelf -h` 和 `ldd` 查看 ELF 和依赖信息。
5. 可选执行一次课程指定 benchmark，记录环境、命令和输出；如果不执行，写明原因。
6. 如果运行失败，记录失败命令、返回值、错误输出和排查步骤。

## 运行验证

| 验证项 | 预期现象 | 是否通过 |
| --- | --- | --- |
| 程序运行 | 输出 Hello World |  |
| `file` | 能看到文件类型和目标架构 |  |
| `readelf` | 能看到目标架构 |  |
| `ldd` | 能看到依赖库或说明静态链接 |  |
| benchmark 边界 | 能说明 benchmark 是选做记录，不作为单次性能结论 |  |
| 调试记录 | 有失败、判断或差异说明 |  |

## 提交要求

- 源码。
- 编译命令。
- 运行、观察和失败排查结果。
